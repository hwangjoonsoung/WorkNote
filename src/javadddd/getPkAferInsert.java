package javadddd;

public class getPkAferInsert {
    /*
        stmt.executeUpdate(sql, Statement.RETURN_GENERATED_KEYS);

        int PK = -1;

        Rs = stmt.getGeneratedKeys();
        if(Rs.next()){
            PK = Rs.getInt(1);
        }

     */
}
