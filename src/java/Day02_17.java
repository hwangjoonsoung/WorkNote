package java;

import java.util.Enumeration;
import java.util.Iterator;

public class Day02_17 {

    /*
    q1.이게 뭐야?
    Enumeration enumber = request.getParameterNames();
    Enumeration class는 iterator와 비슷한 기능을 한다.
    차이는 iterator은 remove가 가능하지만 enumeration은 remove가 불가능하다.

    q2. orm을 여러개 사용할수도 있지 않을까?
    
    q3. DB에 datatype이 varchar인 컬럼에 아무것도 입력이 안되어 있있는 경우 get("컬럼")을 하면 null을 가져올까? ""을 가져올까?
    예상 : varchar의 기본값을 ""이니 get("컬럼")을 하면 빈 문자열을 가져올 것이라고 생각했다.
    정답 : 아직 확인 안해봄
    


    */
}
