package java;

import java.util.HashSet;

public class attrAndProp {

        /*
            .attr()을 통해서는 element가 가지는 속성값이나 정보를 조회(style, src, rowspan 등)하거나 세팅하는 형식,

            .prop()을 통해서는 element가 가지는 실제적인 상태(활성화, 체크, 선택여부 등)를 제어하는 업무

            결론적으로 css를 변경할 때에는 attr을 사용하고 ed로 끝나는 것을 사용하는 것은 prop로 한다.


<script>
    var deletedFilename = ""

    function removeFile(id) {
        var id = "#" + id + "";
        deletedFilename = deletedFilename + id + ",";
        $("#_deletedFiles").val(deletedFilename);
        $(id).attr("style", "text-decoration : line-through ")
    }
</script>
         */

}
